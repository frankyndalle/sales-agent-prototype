[project]
name = "sales-agent-prototype"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "frankyndalle", email = "difrank2@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.15",
    "langchain-groq>=1.1.3",
    "langchain-openai>=1.4.3",
    "python-dotenv>=1.2.2",
    "streamlit>=1.61.1",
]

[project.scripts]
sales-agent-prototype = "sales_agent_prototype:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
